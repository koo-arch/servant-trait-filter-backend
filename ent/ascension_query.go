// Code generated by ent, DO NOT EDIT.

package ent

import (
	"context"
	"fmt"
	"math"

	"entgo.io/ent"
	"entgo.io/ent/dialect/sql"
	"entgo.io/ent/dialect/sql/sqlgraph"
	"entgo.io/ent/schema/field"
	"github.com/koo-arch/servant-trait-filter-backend/ent/ascension"
	"github.com/koo-arch/servant-trait-filter-backend/ent/attribute"
	"github.com/koo-arch/servant-trait-filter-backend/ent/moralalignment"
	"github.com/koo-arch/servant-trait-filter-backend/ent/orderalignment"
	"github.com/koo-arch/servant-trait-filter-backend/ent/predicate"
	"github.com/koo-arch/servant-trait-filter-backend/ent/servant"
)

// AscensionQuery is the builder for querying Ascension entities.
type AscensionQuery struct {
	config
	ctx                *QueryContext
	order              []ascension.OrderOption
	inters             []Interceptor
	predicates         []predicate.Ascension
	withServant        *ServantQuery
	withAttribute      *AttributeQuery
	withOrderAlignment *OrderAlignmentQuery
	withMoralAlignment *MoralAlignmentQuery
	// intermediate query (i.e. traversal path).
	sql  *sql.Selector
	path func(context.Context) (*sql.Selector, error)
}

// Where adds a new predicate for the AscensionQuery builder.
func (aq *AscensionQuery) Where(ps ...predicate.Ascension) *AscensionQuery {
	aq.predicates = append(aq.predicates, ps...)
	return aq
}

// Limit the number of records to be returned by this query.
func (aq *AscensionQuery) Limit(limit int) *AscensionQuery {
	aq.ctx.Limit = &limit
	return aq
}

// Offset to start from.
func (aq *AscensionQuery) Offset(offset int) *AscensionQuery {
	aq.ctx.Offset = &offset
	return aq
}

// Unique configures the query builder to filter duplicate records on query.
// By default, unique is set to true, and can be disabled using this method.
func (aq *AscensionQuery) Unique(unique bool) *AscensionQuery {
	aq.ctx.Unique = &unique
	return aq
}

// Order specifies how the records should be ordered.
func (aq *AscensionQuery) Order(o ...ascension.OrderOption) *AscensionQuery {
	aq.order = append(aq.order, o...)
	return aq
}

// QueryServant chains the current query on the "servant" edge.
func (aq *AscensionQuery) QueryServant() *ServantQuery {
	query := (&ServantClient{config: aq.config}).Query()
	query.path = func(ctx context.Context) (fromU *sql.Selector, err error) {
		if err := aq.prepareQuery(ctx); err != nil {
			return nil, err
		}
		selector := aq.sqlQuery(ctx)
		if err := selector.Err(); err != nil {
			return nil, err
		}
		step := sqlgraph.NewStep(
			sqlgraph.From(ascension.Table, ascension.FieldID, selector),
			sqlgraph.To(servant.Table, servant.FieldID),
			sqlgraph.Edge(sqlgraph.M2O, true, ascension.ServantTable, ascension.ServantColumn),
		)
		fromU = sqlgraph.SetNeighbors(aq.driver.Dialect(), step)
		return fromU, nil
	}
	return query
}

// QueryAttribute chains the current query on the "attribute" edge.
func (aq *AscensionQuery) QueryAttribute() *AttributeQuery {
	query := (&AttributeClient{config: aq.config}).Query()
	query.path = func(ctx context.Context) (fromU *sql.Selector, err error) {
		if err := aq.prepareQuery(ctx); err != nil {
			return nil, err
		}
		selector := aq.sqlQuery(ctx)
		if err := selector.Err(); err != nil {
			return nil, err
		}
		step := sqlgraph.NewStep(
			sqlgraph.From(ascension.Table, ascension.FieldID, selector),
			sqlgraph.To(attribute.Table, attribute.FieldID),
			sqlgraph.Edge(sqlgraph.M2O, true, ascension.AttributeTable, ascension.AttributeColumn),
		)
		fromU = sqlgraph.SetNeighbors(aq.driver.Dialect(), step)
		return fromU, nil
	}
	return query
}

// QueryOrderAlignment chains the current query on the "order_alignment" edge.
func (aq *AscensionQuery) QueryOrderAlignment() *OrderAlignmentQuery {
	query := (&OrderAlignmentClient{config: aq.config}).Query()
	query.path = func(ctx context.Context) (fromU *sql.Selector, err error) {
		if err := aq.prepareQuery(ctx); err != nil {
			return nil, err
		}
		selector := aq.sqlQuery(ctx)
		if err := selector.Err(); err != nil {
			return nil, err
		}
		step := sqlgraph.NewStep(
			sqlgraph.From(ascension.Table, ascension.FieldID, selector),
			sqlgraph.To(orderalignment.Table, orderalignment.FieldID),
			sqlgraph.Edge(sqlgraph.M2O, true, ascension.OrderAlignmentTable, ascension.OrderAlignmentColumn),
		)
		fromU = sqlgraph.SetNeighbors(aq.driver.Dialect(), step)
		return fromU, nil
	}
	return query
}

// QueryMoralAlignment chains the current query on the "moral_alignment" edge.
func (aq *AscensionQuery) QueryMoralAlignment() *MoralAlignmentQuery {
	query := (&MoralAlignmentClient{config: aq.config}).Query()
	query.path = func(ctx context.Context) (fromU *sql.Selector, err error) {
		if err := aq.prepareQuery(ctx); err != nil {
			return nil, err
		}
		selector := aq.sqlQuery(ctx)
		if err := selector.Err(); err != nil {
			return nil, err
		}
		step := sqlgraph.NewStep(
			sqlgraph.From(ascension.Table, ascension.FieldID, selector),
			sqlgraph.To(moralalignment.Table, moralalignment.FieldID),
			sqlgraph.Edge(sqlgraph.M2O, true, ascension.MoralAlignmentTable, ascension.MoralAlignmentColumn),
		)
		fromU = sqlgraph.SetNeighbors(aq.driver.Dialect(), step)
		return fromU, nil
	}
	return query
}

// First returns the first Ascension entity from the query.
// Returns a *NotFoundError when no Ascension was found.
func (aq *AscensionQuery) First(ctx context.Context) (*Ascension, error) {
	nodes, err := aq.Limit(1).All(setContextOp(ctx, aq.ctx, ent.OpQueryFirst))
	if err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nil, &NotFoundError{ascension.Label}
	}
	return nodes[0], nil
}

// FirstX is like First, but panics if an error occurs.
func (aq *AscensionQuery) FirstX(ctx context.Context) *Ascension {
	node, err := aq.First(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return node
}

// FirstID returns the first Ascension ID from the query.
// Returns a *NotFoundError when no Ascension ID was found.
func (aq *AscensionQuery) FirstID(ctx context.Context) (id int, err error) {
	var ids []int
	if ids, err = aq.Limit(1).IDs(setContextOp(ctx, aq.ctx, ent.OpQueryFirstID)); err != nil {
		return
	}
	if len(ids) == 0 {
		err = &NotFoundError{ascension.Label}
		return
	}
	return ids[0], nil
}

// FirstIDX is like FirstID, but panics if an error occurs.
func (aq *AscensionQuery) FirstIDX(ctx context.Context) int {
	id, err := aq.FirstID(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return id
}

// Only returns a single Ascension entity found by the query, ensuring it only returns one.
// Returns a *NotSingularError when more than one Ascension entity is found.
// Returns a *NotFoundError when no Ascension entities are found.
func (aq *AscensionQuery) Only(ctx context.Context) (*Ascension, error) {
	nodes, err := aq.Limit(2).All(setContextOp(ctx, aq.ctx, ent.OpQueryOnly))
	if err != nil {
		return nil, err
	}
	switch len(nodes) {
	case 1:
		return nodes[0], nil
	case 0:
		return nil, &NotFoundError{ascension.Label}
	default:
		return nil, &NotSingularError{ascension.Label}
	}
}

// OnlyX is like Only, but panics if an error occurs.
func (aq *AscensionQuery) OnlyX(ctx context.Context) *Ascension {
	node, err := aq.Only(ctx)
	if err != nil {
		panic(err)
	}
	return node
}

// OnlyID is like Only, but returns the only Ascension ID in the query.
// Returns a *NotSingularError when more than one Ascension ID is found.
// Returns a *NotFoundError when no entities are found.
func (aq *AscensionQuery) OnlyID(ctx context.Context) (id int, err error) {
	var ids []int
	if ids, err = aq.Limit(2).IDs(setContextOp(ctx, aq.ctx, ent.OpQueryOnlyID)); err != nil {
		return
	}
	switch len(ids) {
	case 1:
		id = ids[0]
	case 0:
		err = &NotFoundError{ascension.Label}
	default:
		err = &NotSingularError{ascension.Label}
	}
	return
}

// OnlyIDX is like OnlyID, but panics if an error occurs.
func (aq *AscensionQuery) OnlyIDX(ctx context.Context) int {
	id, err := aq.OnlyID(ctx)
	if err != nil {
		panic(err)
	}
	return id
}

// All executes the query and returns a list of Ascensions.
func (aq *AscensionQuery) All(ctx context.Context) ([]*Ascension, error) {
	ctx = setContextOp(ctx, aq.ctx, ent.OpQueryAll)
	if err := aq.prepareQuery(ctx); err != nil {
		return nil, err
	}
	qr := querierAll[[]*Ascension, *AscensionQuery]()
	return withInterceptors[[]*Ascension](ctx, aq, qr, aq.inters)
}

// AllX is like All, but panics if an error occurs.
func (aq *AscensionQuery) AllX(ctx context.Context) []*Ascension {
	nodes, err := aq.All(ctx)
	if err != nil {
		panic(err)
	}
	return nodes
}

// IDs executes the query and returns a list of Ascension IDs.
func (aq *AscensionQuery) IDs(ctx context.Context) (ids []int, err error) {
	if aq.ctx.Unique == nil && aq.path != nil {
		aq.Unique(true)
	}
	ctx = setContextOp(ctx, aq.ctx, ent.OpQueryIDs)
	if err = aq.Select(ascension.FieldID).Scan(ctx, &ids); err != nil {
		return nil, err
	}
	return ids, nil
}

// IDsX is like IDs, but panics if an error occurs.
func (aq *AscensionQuery) IDsX(ctx context.Context) []int {
	ids, err := aq.IDs(ctx)
	if err != nil {
		panic(err)
	}
	return ids
}

// Count returns the count of the given query.
func (aq *AscensionQuery) Count(ctx context.Context) (int, error) {
	ctx = setContextOp(ctx, aq.ctx, ent.OpQueryCount)
	if err := aq.prepareQuery(ctx); err != nil {
		return 0, err
	}
	return withInterceptors[int](ctx, aq, querierCount[*AscensionQuery](), aq.inters)
}

// CountX is like Count, but panics if an error occurs.
func (aq *AscensionQuery) CountX(ctx context.Context) int {
	count, err := aq.Count(ctx)
	if err != nil {
		panic(err)
	}
	return count
}

// Exist returns true if the query has elements in the graph.
func (aq *AscensionQuery) Exist(ctx context.Context) (bool, error) {
	ctx = setContextOp(ctx, aq.ctx, ent.OpQueryExist)
	switch _, err := aq.FirstID(ctx); {
	case IsNotFound(err):
		return false, nil
	case err != nil:
		return false, fmt.Errorf("ent: check existence: %w", err)
	default:
		return true, nil
	}
}

// ExistX is like Exist, but panics if an error occurs.
func (aq *AscensionQuery) ExistX(ctx context.Context) bool {
	exist, err := aq.Exist(ctx)
	if err != nil {
		panic(err)
	}
	return exist
}

// Clone returns a duplicate of the AscensionQuery builder, including all associated steps. It can be
// used to prepare common query builders and use them differently after the clone is made.
func (aq *AscensionQuery) Clone() *AscensionQuery {
	if aq == nil {
		return nil
	}
	return &AscensionQuery{
		config:             aq.config,
		ctx:                aq.ctx.Clone(),
		order:              append([]ascension.OrderOption{}, aq.order...),
		inters:             append([]Interceptor{}, aq.inters...),
		predicates:         append([]predicate.Ascension{}, aq.predicates...),
		withServant:        aq.withServant.Clone(),
		withAttribute:      aq.withAttribute.Clone(),
		withOrderAlignment: aq.withOrderAlignment.Clone(),
		withMoralAlignment: aq.withMoralAlignment.Clone(),
		// clone intermediate query.
		sql:  aq.sql.Clone(),
		path: aq.path,
	}
}

// WithServant tells the query-builder to eager-load the nodes that are connected to
// the "servant" edge. The optional arguments are used to configure the query builder of the edge.
func (aq *AscensionQuery) WithServant(opts ...func(*ServantQuery)) *AscensionQuery {
	query := (&ServantClient{config: aq.config}).Query()
	for _, opt := range opts {
		opt(query)
	}
	aq.withServant = query
	return aq
}

// WithAttribute tells the query-builder to eager-load the nodes that are connected to
// the "attribute" edge. The optional arguments are used to configure the query builder of the edge.
func (aq *AscensionQuery) WithAttribute(opts ...func(*AttributeQuery)) *AscensionQuery {
	query := (&AttributeClient{config: aq.config}).Query()
	for _, opt := range opts {
		opt(query)
	}
	aq.withAttribute = query
	return aq
}

// WithOrderAlignment tells the query-builder to eager-load the nodes that are connected to
// the "order_alignment" edge. The optional arguments are used to configure the query builder of the edge.
func (aq *AscensionQuery) WithOrderAlignment(opts ...func(*OrderAlignmentQuery)) *AscensionQuery {
	query := (&OrderAlignmentClient{config: aq.config}).Query()
	for _, opt := range opts {
		opt(query)
	}
	aq.withOrderAlignment = query
	return aq
}

// WithMoralAlignment tells the query-builder to eager-load the nodes that are connected to
// the "moral_alignment" edge. The optional arguments are used to configure the query builder of the edge.
func (aq *AscensionQuery) WithMoralAlignment(opts ...func(*MoralAlignmentQuery)) *AscensionQuery {
	query := (&MoralAlignmentClient{config: aq.config}).Query()
	for _, opt := range opts {
		opt(query)
	}
	aq.withMoralAlignment = query
	return aq
}

// GroupBy is used to group vertices by one or more fields/columns.
// It is often used with aggregate functions, like: count, max, mean, min, sum.
//
// Example:
//
//	var v []struct {
//		CreatedAt time.Time `json:"created_at,omitempty"`
//		Count int `json:"count,omitempty"`
//	}
//
//	client.Ascension.Query().
//		GroupBy(ascension.FieldCreatedAt).
//		Aggregate(ent.Count()).
//		Scan(ctx, &v)
func (aq *AscensionQuery) GroupBy(field string, fields ...string) *AscensionGroupBy {
	aq.ctx.Fields = append([]string{field}, fields...)
	grbuild := &AscensionGroupBy{build: aq}
	grbuild.flds = &aq.ctx.Fields
	grbuild.label = ascension.Label
	grbuild.scan = grbuild.Scan
	return grbuild
}

// Select allows the selection one or more fields/columns for the given query,
// instead of selecting all fields in the entity.
//
// Example:
//
//	var v []struct {
//		CreatedAt time.Time `json:"created_at,omitempty"`
//	}
//
//	client.Ascension.Query().
//		Select(ascension.FieldCreatedAt).
//		Scan(ctx, &v)
func (aq *AscensionQuery) Select(fields ...string) *AscensionSelect {
	aq.ctx.Fields = append(aq.ctx.Fields, fields...)
	sbuild := &AscensionSelect{AscensionQuery: aq}
	sbuild.label = ascension.Label
	sbuild.flds, sbuild.scan = &aq.ctx.Fields, sbuild.Scan
	return sbuild
}

// Aggregate returns a AscensionSelect configured with the given aggregations.
func (aq *AscensionQuery) Aggregate(fns ...AggregateFunc) *AscensionSelect {
	return aq.Select().Aggregate(fns...)
}

func (aq *AscensionQuery) prepareQuery(ctx context.Context) error {
	for _, inter := range aq.inters {
		if inter == nil {
			return fmt.Errorf("ent: uninitialized interceptor (forgotten import ent/runtime?)")
		}
		if trv, ok := inter.(Traverser); ok {
			if err := trv.Traverse(ctx, aq); err != nil {
				return err
			}
		}
	}
	for _, f := range aq.ctx.Fields {
		if !ascension.ValidColumn(f) {
			return &ValidationError{Name: f, err: fmt.Errorf("ent: invalid field %q for query", f)}
		}
	}
	if aq.path != nil {
		prev, err := aq.path(ctx)
		if err != nil {
			return err
		}
		aq.sql = prev
	}
	return nil
}

func (aq *AscensionQuery) sqlAll(ctx context.Context, hooks ...queryHook) ([]*Ascension, error) {
	var (
		nodes       = []*Ascension{}
		_spec       = aq.querySpec()
		loadedTypes = [4]bool{
			aq.withServant != nil,
			aq.withAttribute != nil,
			aq.withOrderAlignment != nil,
			aq.withMoralAlignment != nil,
		}
	)
	_spec.ScanValues = func(columns []string) ([]any, error) {
		return (*Ascension).scanValues(nil, columns)
	}
	_spec.Assign = func(columns []string, values []any) error {
		node := &Ascension{config: aq.config}
		nodes = append(nodes, node)
		node.Edges.loadedTypes = loadedTypes
		return node.assignValues(columns, values)
	}
	for i := range hooks {
		hooks[i](ctx, _spec)
	}
	if err := sqlgraph.QueryNodes(ctx, aq.driver, _spec); err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nodes, nil
	}
	if query := aq.withServant; query != nil {
		if err := aq.loadServant(ctx, query, nodes, nil,
			func(n *Ascension, e *Servant) { n.Edges.Servant = e }); err != nil {
			return nil, err
		}
	}
	if query := aq.withAttribute; query != nil {
		if err := aq.loadAttribute(ctx, query, nodes, nil,
			func(n *Ascension, e *Attribute) { n.Edges.Attribute = e }); err != nil {
			return nil, err
		}
	}
	if query := aq.withOrderAlignment; query != nil {
		if err := aq.loadOrderAlignment(ctx, query, nodes, nil,
			func(n *Ascension, e *OrderAlignment) { n.Edges.OrderAlignment = e }); err != nil {
			return nil, err
		}
	}
	if query := aq.withMoralAlignment; query != nil {
		if err := aq.loadMoralAlignment(ctx, query, nodes, nil,
			func(n *Ascension, e *MoralAlignment) { n.Edges.MoralAlignment = e }); err != nil {
			return nil, err
		}
	}
	return nodes, nil
}

func (aq *AscensionQuery) loadServant(ctx context.Context, query *ServantQuery, nodes []*Ascension, init func(*Ascension), assign func(*Ascension, *Servant)) error {
	ids := make([]int, 0, len(nodes))
	nodeids := make(map[int][]*Ascension)
	for i := range nodes {
		fk := nodes[i].ServantID
		if _, ok := nodeids[fk]; !ok {
			ids = append(ids, fk)
		}
		nodeids[fk] = append(nodeids[fk], nodes[i])
	}
	if len(ids) == 0 {
		return nil
	}
	query.Where(servant.IDIn(ids...))
	neighbors, err := query.All(ctx)
	if err != nil {
		return err
	}
	for _, n := range neighbors {
		nodes, ok := nodeids[n.ID]
		if !ok {
			return fmt.Errorf(`unexpected foreign-key "servant_id" returned %v`, n.ID)
		}
		for i := range nodes {
			assign(nodes[i], n)
		}
	}
	return nil
}
func (aq *AscensionQuery) loadAttribute(ctx context.Context, query *AttributeQuery, nodes []*Ascension, init func(*Ascension), assign func(*Ascension, *Attribute)) error {
	ids := make([]int, 0, len(nodes))
	nodeids := make(map[int][]*Ascension)
	for i := range nodes {
		fk := nodes[i].AttributeID
		if _, ok := nodeids[fk]; !ok {
			ids = append(ids, fk)
		}
		nodeids[fk] = append(nodeids[fk], nodes[i])
	}
	if len(ids) == 0 {
		return nil
	}
	query.Where(attribute.IDIn(ids...))
	neighbors, err := query.All(ctx)
	if err != nil {
		return err
	}
	for _, n := range neighbors {
		nodes, ok := nodeids[n.ID]
		if !ok {
			return fmt.Errorf(`unexpected foreign-key "attribute_id" returned %v`, n.ID)
		}
		for i := range nodes {
			assign(nodes[i], n)
		}
	}
	return nil
}
func (aq *AscensionQuery) loadOrderAlignment(ctx context.Context, query *OrderAlignmentQuery, nodes []*Ascension, init func(*Ascension), assign func(*Ascension, *OrderAlignment)) error {
	ids := make([]int, 0, len(nodes))
	nodeids := make(map[int][]*Ascension)
	for i := range nodes {
		fk := nodes[i].OrderAlignmentID
		if _, ok := nodeids[fk]; !ok {
			ids = append(ids, fk)
		}
		nodeids[fk] = append(nodeids[fk], nodes[i])
	}
	if len(ids) == 0 {
		return nil
	}
	query.Where(orderalignment.IDIn(ids...))
	neighbors, err := query.All(ctx)
	if err != nil {
		return err
	}
	for _, n := range neighbors {
		nodes, ok := nodeids[n.ID]
		if !ok {
			return fmt.Errorf(`unexpected foreign-key "order_alignment_id" returned %v`, n.ID)
		}
		for i := range nodes {
			assign(nodes[i], n)
		}
	}
	return nil
}
func (aq *AscensionQuery) loadMoralAlignment(ctx context.Context, query *MoralAlignmentQuery, nodes []*Ascension, init func(*Ascension), assign func(*Ascension, *MoralAlignment)) error {
	ids := make([]int, 0, len(nodes))
	nodeids := make(map[int][]*Ascension)
	for i := range nodes {
		fk := nodes[i].MoralAlignmentID
		if _, ok := nodeids[fk]; !ok {
			ids = append(ids, fk)
		}
		nodeids[fk] = append(nodeids[fk], nodes[i])
	}
	if len(ids) == 0 {
		return nil
	}
	query.Where(moralalignment.IDIn(ids...))
	neighbors, err := query.All(ctx)
	if err != nil {
		return err
	}
	for _, n := range neighbors {
		nodes, ok := nodeids[n.ID]
		if !ok {
			return fmt.Errorf(`unexpected foreign-key "moral_alignment_id" returned %v`, n.ID)
		}
		for i := range nodes {
			assign(nodes[i], n)
		}
	}
	return nil
}

func (aq *AscensionQuery) sqlCount(ctx context.Context) (int, error) {
	_spec := aq.querySpec()
	_spec.Node.Columns = aq.ctx.Fields
	if len(aq.ctx.Fields) > 0 {
		_spec.Unique = aq.ctx.Unique != nil && *aq.ctx.Unique
	}
	return sqlgraph.CountNodes(ctx, aq.driver, _spec)
}

func (aq *AscensionQuery) querySpec() *sqlgraph.QuerySpec {
	_spec := sqlgraph.NewQuerySpec(ascension.Table, ascension.Columns, sqlgraph.NewFieldSpec(ascension.FieldID, field.TypeInt))
	_spec.From = aq.sql
	if unique := aq.ctx.Unique; unique != nil {
		_spec.Unique = *unique
	} else if aq.path != nil {
		_spec.Unique = true
	}
	if fields := aq.ctx.Fields; len(fields) > 0 {
		_spec.Node.Columns = make([]string, 0, len(fields))
		_spec.Node.Columns = append(_spec.Node.Columns, ascension.FieldID)
		for i := range fields {
			if fields[i] != ascension.FieldID {
				_spec.Node.Columns = append(_spec.Node.Columns, fields[i])
			}
		}
		if aq.withServant != nil {
			_spec.Node.AddColumnOnce(ascension.FieldServantID)
		}
		if aq.withAttribute != nil {
			_spec.Node.AddColumnOnce(ascension.FieldAttributeID)
		}
		if aq.withOrderAlignment != nil {
			_spec.Node.AddColumnOnce(ascension.FieldOrderAlignmentID)
		}
		if aq.withMoralAlignment != nil {
			_spec.Node.AddColumnOnce(ascension.FieldMoralAlignmentID)
		}
	}
	if ps := aq.predicates; len(ps) > 0 {
		_spec.Predicate = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	if limit := aq.ctx.Limit; limit != nil {
		_spec.Limit = *limit
	}
	if offset := aq.ctx.Offset; offset != nil {
		_spec.Offset = *offset
	}
	if ps := aq.order; len(ps) > 0 {
		_spec.Order = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	return _spec
}

func (aq *AscensionQuery) sqlQuery(ctx context.Context) *sql.Selector {
	builder := sql.Dialect(aq.driver.Dialect())
	t1 := builder.Table(ascension.Table)
	columns := aq.ctx.Fields
	if len(columns) == 0 {
		columns = ascension.Columns
	}
	selector := builder.Select(t1.Columns(columns...)...).From(t1)
	if aq.sql != nil {
		selector = aq.sql
		selector.Select(selector.Columns(columns...)...)
	}
	if aq.ctx.Unique != nil && *aq.ctx.Unique {
		selector.Distinct()
	}
	for _, p := range aq.predicates {
		p(selector)
	}
	for _, p := range aq.order {
		p(selector)
	}
	if offset := aq.ctx.Offset; offset != nil {
		// limit is mandatory for offset clause. We start
		// with default value, and override it below if needed.
		selector.Offset(*offset).Limit(math.MaxInt32)
	}
	if limit := aq.ctx.Limit; limit != nil {
		selector.Limit(*limit)
	}
	return selector
}

// AscensionGroupBy is the group-by builder for Ascension entities.
type AscensionGroupBy struct {
	selector
	build *AscensionQuery
}

// Aggregate adds the given aggregation functions to the group-by query.
func (agb *AscensionGroupBy) Aggregate(fns ...AggregateFunc) *AscensionGroupBy {
	agb.fns = append(agb.fns, fns...)
	return agb
}

// Scan applies the selector query and scans the result into the given value.
func (agb *AscensionGroupBy) Scan(ctx context.Context, v any) error {
	ctx = setContextOp(ctx, agb.build.ctx, ent.OpQueryGroupBy)
	if err := agb.build.prepareQuery(ctx); err != nil {
		return err
	}
	return scanWithInterceptors[*AscensionQuery, *AscensionGroupBy](ctx, agb.build, agb, agb.build.inters, v)
}

func (agb *AscensionGroupBy) sqlScan(ctx context.Context, root *AscensionQuery, v any) error {
	selector := root.sqlQuery(ctx).Select()
	aggregation := make([]string, 0, len(agb.fns))
	for _, fn := range agb.fns {
		aggregation = append(aggregation, fn(selector))
	}
	if len(selector.SelectedColumns()) == 0 {
		columns := make([]string, 0, len(*agb.flds)+len(agb.fns))
		for _, f := range *agb.flds {
			columns = append(columns, selector.C(f))
		}
		columns = append(columns, aggregation...)
		selector.Select(columns...)
	}
	selector.GroupBy(selector.Columns(*agb.flds...)...)
	if err := selector.Err(); err != nil {
		return err
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := agb.build.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}

// AscensionSelect is the builder for selecting fields of Ascension entities.
type AscensionSelect struct {
	*AscensionQuery
	selector
}

// Aggregate adds the given aggregation functions to the selector query.
func (as *AscensionSelect) Aggregate(fns ...AggregateFunc) *AscensionSelect {
	as.fns = append(as.fns, fns...)
	return as
}

// Scan applies the selector query and scans the result into the given value.
func (as *AscensionSelect) Scan(ctx context.Context, v any) error {
	ctx = setContextOp(ctx, as.ctx, ent.OpQuerySelect)
	if err := as.prepareQuery(ctx); err != nil {
		return err
	}
	return scanWithInterceptors[*AscensionQuery, *AscensionSelect](ctx, as.AscensionQuery, as, as.inters, v)
}

func (as *AscensionSelect) sqlScan(ctx context.Context, root *AscensionQuery, v any) error {
	selector := root.sqlQuery(ctx)
	aggregation := make([]string, 0, len(as.fns))
	for _, fn := range as.fns {
		aggregation = append(aggregation, fn(selector))
	}
	switch n := len(*as.selector.flds); {
	case n == 0 && len(aggregation) > 0:
		selector.Select(aggregation...)
	case n != 0 && len(aggregation) > 0:
		selector.AppendSelect(aggregation...)
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := as.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}
